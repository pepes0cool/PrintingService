@model User

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy tokens - Success</title>
</head>
<body>
        <div class="success-icon">✔</div>
        <div class="success-message">
            Your purchase was successful! Thank you for buying pages.
        </div>
        <div class="balance">
            Updated Balance: <strong>@Model.PaperBalance</strong> pages
        </div>
        <a class="back-button" asp-controller="Home" asp-action="Index" asp-area="Student">Back to Home</a>
</body>
</html>

    <style>
        .container {
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px 30px;
            text-align: center;
            max-width: 350px;
        }
        .success-icon {
            font-size: 50px;
            color: #4caf50;
            margin-bottom: 20px;
        }

        .success-message {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .balance {
            font-size: 16px;
            margin-bottom: 20px;
            color: #555;
        }

        .balance strong {
            color: #333;
        }

        .back-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4caf50;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .back-button:hover {
            background-color: #45a049;
        }
    </style>